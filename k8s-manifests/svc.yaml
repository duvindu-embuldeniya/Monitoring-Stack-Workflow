apiVersion: v1
kind: Service
metadata:
  name: django-svc
  labels:
    job: django-app
    app: django          
spec:
  type: ClusterIP
  selector:
    app: django
  ports:
    - name: web          
      port: 8000
      targetPort: 8000